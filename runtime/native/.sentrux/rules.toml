# Runtime/native architectural guardrails.
# Current baseline at creation: quality_signal=5159.

[constraints]
min_quality = 0.50
min_modularity = 0.30
min_acyclicity = 1.0
min_equality = 0.45
# Re-placed 2026-07-12 (structural-cleanup epic, task 4): operating point
# 0.2514 after the registry dedup, commit-to-commit noise band 0.0015 -> 
# threshold 3 bands below the operating point. The old 0.25 sat 0.0009
# from the operating point — inside one commit's noise (see
# docs/runtime-v2-epics/15-tasks/01-kickoff-attribution.md).
min_redundancy = 0.245
max_cc = 70
max_fn_lines = 300
