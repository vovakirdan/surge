## 24 — Option Pipeline

Handling optional values safely.

### What it demonstrates
- `Option<T>` (syntax `T?`) usage
- `Some(v)` and `nothing` variants
- `.safe()` method for unwrapping (panics on nothing)
- Returning optional values from functions

### Run
```bash
surge run showcases/24_option_pipeline/main.sg
```
