## 26 — State Machine (Tagged Unions)

A state machine implementation using Sum Types (Tagged Unions).

### What it demonstrates
- Defining tagged unions (`tag` and `type`)
- Pattern matching with `compare`
- State transitions based on events
- Data carried in enum variants

### Run
```bash
surge run showcases/26_state_machine_tagged/main.sg
```
