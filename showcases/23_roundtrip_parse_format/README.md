## 23 — Roundtrip Parse/Format

String to Number and Number to String conversion consistency.

### What it demonstrates
- `int.from_str()` and `float.from_str()`
- `val to string` conversion
- Verifying round-trip correctness (str -> num -> str)
- Handling edge cases like "-0.0"

### Run
```bash
surge run showcases/23_roundtrip_parse_format/main.sg
```
