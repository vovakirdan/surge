## 29 — Contract Printable

Implementing a custom printing behavior.

### What it demonstrates
- Contracts (structural interfaces)
- The `Printable` contract via `__to(string)`
- Implementing `__to` in `extern<T>` block
- Automatic string conversion in `print`

### Run
```bash
surge run showcases/29_contract_printable/main.sg
```
