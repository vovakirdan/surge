@entrypoint
fn main() -> int {
    let mut a: Channel<int>[] = Array::<Channel<int>>::with_len(3:uint);
    a[1] = Channel::<int>::new(1:uint);
    a[1].send(4);
    let v = a[1].recv();
    compare v { Some(x) => print("withlen " + (x to string)); nothing => print("none"); };
    return 0;
}
