async fn hold() -> int {
    let t = default::<Task<int>>();
    checkpoint().await();
    print("after-checkpoint");
    return 4;
}

@entrypoint
fn main() -> int {
    let t = spawn hold();
    return compare t.await() { Success(v) => v; Cancelled() => 90; };
}
