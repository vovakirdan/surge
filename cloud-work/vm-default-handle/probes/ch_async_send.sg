async fn hold() -> int {
    let c = default::<Channel<int>>();
    checkpoint().await();
    print("before");
    c.send(1);
    print("after");
    return 4;
}

@entrypoint
fn main() -> int {
    let t = spawn hold();
    return compare t.await() { Success(v) => v; Cancelled() => 90; };
}
