async fn child() -> int {
    let n = default::<Task<int>>();
    let r = timeout(n, 10:uint);
    print("after-timeout");
    return 1;
}

@entrypoint
fn main() -> int {
    let t = spawn child();
    t.cancel();
    return compare t.await() { Success(v) => v; Cancelled() => 90; };
}
