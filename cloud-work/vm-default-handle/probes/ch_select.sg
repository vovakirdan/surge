async fn pick() -> int {
    let c = default::<Channel<int>>();
    print("before");
    let w: int = select { c.recv() => 10; };
    print("after");
    return w;
}

@entrypoint
fn main() -> int {
    let t = spawn pick();
    return compare t.await() { Success(v) => v; Cancelled() => 90; };
}
