аза A — фронтенд (лексер/парсер/AST)
	•	Лексер
	•	Экрани и форматы литералов: \n \t \\ \", 0x..., 1_000, 1.0e-3.
	•	Унарные операторы: -x, +x, логический !x.
	•	Комментарии // и /*…*/ (включая многострочные).
	•	Точная позиционная информация (line/col) для каждого токена.
	•	Парсер
	•	Приоритеты и ассоциативность всех операторов (включая унарные).
	•	Постфиксный индекс a[b], вызов f(x, y), группировка ( … ).
	•	Блоки/ветвления/циклы: {}, if/else, while.
	•	Присваивания и bind-выражения: x = expr, a[i] := v.
	•	Объявления let, fn (с сигнатурами, возвращаемыми типами).
	•	Диагностика
	•	Формат file:line:col: error: … с подсветкой строки и ^.
	•	Не менее 10 негативных кейсов (неизвестный токен, незакрытая кавычка и т.д.)
	•	Тесты
	•	tests/lexing золотые эталоны.
	•	tests/parsing снапшоты AST.

Фаза B — типы и семантика (строгая проверка)
	•	Базовые типы и массивы: int, float, bool, string, T[].
	•	Таблицы символов, области видимости, запрет “диких” неразрешённых имён.
	•	Проверка сигнатур функций и возвращаемых значений (включая все пути).
	•	Семантика присваивания/индексации: lvalue-проверки, тип элемента массива.
	•	Арифметика/сравнения — правила типов:
	•	Полная поддержка int и float выражений (включая унарный -).
	•	Неявные промоушены int→float в смешанных выражениях.
	•	Запрет операций для несовместимых типов (например, string + int на MVP).
	•	Контроль чистоты (подготовка к F/H):
	•	Аннотация @pure (учёт хотя бы синтаксически).
	•	Блок запретов: запись в глобалки/IO внутри @pure (минимально — флаги на нодах).
	•	Тесты tests/sema
	•	Позитив/негатив на типы, возврат, lvalue, промоушены, @pure запреты.

Фаза C — .sbc и ISA (закрыть вычисления “под ключ”)
	•	Константы: уже есть (I64, F64, STR) — оставить.
	•	Опкоды (добавить к имеющемуся минимуму):
	•	Унарные: NEG_I64, NEG_F64, NOT_BOOL.
	•	Преобразования: I64_TO_F64, F64_TO_I64 (с оговорённой семантикой округления: trunc toward 0).
	•	Полнокомплект арифметики и сравнений для f64: ADD_F64/SUB_F64/MUL_F64/DIV_F64/REM_F64?*,
CMP_EQ_F64/NE/LT/LE/GT/GE (или общий CMP_* с типовым диспатчем в VM — но лучше специализированные).
	•	(Опционально для языка, но полезно): битовые AND/OR/XOR/SHL/SHR для i64.
	•	Формальные правила смешанных выражений на уровне кодогена:
	•	Вставка I64_TO_F64 перед *_F64, либо генерация *_I64/*_F64 на основе типов узлов.
	•	Дизассемблер: распознавание новых опкодов.
	•	Документ docs/BYTECODE.md: дописать таблицу опкодов, размеры операндов, семантику деления/остатка и NaN-сравнений.
	•	Чётко зафиксировать: деление на ноль (int → trap, float → ±inf по IEEE, остаток для float — либо не поддерживаем, либо fmod).

* Если не хотим REM_F64 — прямо запретить на уровне семантики и кодогена.

Фаза D — VM и память (завершение базиса)
	•	VM-цикл
	•	Реализация новых опкодов: унарные, конверсии, полный набор *_F64.
	•	Смешанные выражения: автоматика через конверсии или отказ — в точности как решено в C.
	•	Сравнения float: корректная семантика с NaN (==/!=/<…>).
	•	Строгое сообщение об ошибке: функция+offset уже есть, сохранить для всех новых веток.
	•	Лимиты и трапы:
	•	Лимит глубины фреймов → STACK_OVERFLOW (новый trap-код).
	•	(Опционально) лимит стека значений.
	•	Память
	•	Уже есть: арена, RC-heap для строк/массивов, интернинг строк констант, globals.
	•	Добить углы:
	•	Безопасный retain/release для строк в VmValue (сейчас добавлено — проверить все места копий/assign).
	•	Чистые освобождения при RET, HALT, ошибках/трапах (покрыто — добавить тесты).
	•	(Опционально) небольшие пулы для маленьких массивов (перф в будущем).
	•	Глобалы/инициализация
	•	Есть __global_init_auto__ + __init__ — оставить, покрыть тестами:
	•	порядок вызова: global_init → __init__ → main.
	•	Кодоген
	•	Эмиссия всех новых опкодов (унарные/float/конверсии).
	•	Генерация правильных *_F64 при float выражениях и вставка конверсий при смешанных.
	•	Тесты tests/vm
	•	int/float арифметика (включая унарный -).
	•	Смешанные выражения: 1 + 2.5, 3 < 4.0, и т.п.
	•	Деление на ноль: int → trap; float → IEEE (и сравнения с inf/NaN).
	•	Сравнения всех видов для i64 и f64.
	•	Рекурсия и лимит стека (форсируем overflow).
	•	Глобалы + __init__, строки (intern), массивы (GET/SET/LEN) и OOB-trap.
	•	Негативные типовые TRAP-кейсы (не тот тип операнда).
	•	CLI и DX
	•	surge runbc с флагом --trace.
	•	Валидатор .sbc на загрузке (у нас уже есть базовые проверки) — оставить.
	•	Документация docs/RUNTIME.md
	•	Семантика арифметики/сравнений, трапы, лимиты, модель памяти (арена+RC), глобальный init.
