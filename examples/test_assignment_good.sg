fn test_assignment() {
    let mut x: int = 42;
    let mut arr: int[] = [1, 2, 3];

    // Valid assignments
    x = 10;
    arr[0] = 5;

    // Valid compound assignments
    x = x + 1;
    arr[1] = arr[0] + 2;
}