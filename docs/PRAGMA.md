# Pragma

`pragma` в Surge задумывается не как «магический ключ» (как в C/C++), а как **встроенный механизм объявления “особых свойств модуля” или “режимов компиляции”**.
То есть pragma — это **метадиректива для компилятора**, не влияющая на язык как таковой, но влияющая на то, **как этот файл трактуется, компилируется и собирается**.

---

## 🎯 Общая концепция `pragma`

> **pragma = декларация свойств текущего файла / модуля**,
> читаемая только фронтендом и не попадающая в AST как обычный узел.

* Располагается **в самом верху файла**, до любых объявлений.
* Допускается **только одна pragma** на файл, но может содержать **несколько ключей**:

  ```sg
  pragma directive, no_std, strict
  ```
* Парсер читает pragma в отдельную структуру `PragmaMeta { flags: Vec<PragmaFlag> }`.
* При `--emit-ast` можно увидеть её как `pragma([...])`, но в обычной сборке не попадает в код.

---

## ⚙️ Планируемые виды pragma (M0 → M2)

### ✅ 1) `pragma directive`

> Помечает модуль как **директивный**.
> (уже согласовано)

Используется для:

* test/lint/bench/doc директив,
* позволяет импортировать модуль как пространство директив (`/// test:` и т.п.).

---

### 🧩 2) `pragma module`

> Явное объявление типа модуля, если нам нужно, чтобы Surge знал, **для чего** этот файл.

Примеры:

```sg
pragma module library     // обычный модуль, доступный для импорта
pragma module binary      // точка входа, имеет fn main()
pragma module plugin      // внешнее расширение (например, dynamic .so)
```

Будущее использование:

* CLI (`surge build --module=binary`) будет проверять соответствие.
* Можно будет включать/выключать модули определённого типа при сборке (`--skip plugins` и т.п.).

---

### 🧱 3) `pragma no_std`

> Аналог rust-подобного `#![no_std]`.
> Говорит, что модуль **не должен зависеть от стандартной библиотеки**.

Назначение:

* минимальные бинарные сборки (firmware, runtime),
* ядро Surge (bootstrap).

Компилятор:

* заменяет `stdlib::` на `core::`,
* запрещает импорт `stdlib::*`,
* требует явного `import core::*`.
* в многофайловом модуле значение `no_std` должно совпадать во всех файлах; смешение с файлами без `no_std` → ошибка (PRJ5011).
* стандартный модуль `core` оформлен как многофайловый (`core/*.sg`) c `pragma module, no_std;`: все файлы директории образуют один модуль.

---

### 🧭 4) `pragma strict`

> Включает строгий режим компиляции (анализатор).

Эффекты:

* все warning → error,
* требуется явное указание видимости (`pub` / private),
* запрещает неиспользуемые импорты/переменные,
* отключает неявные приведения типов.

---

### ⚙️ 5) `pragma unsafe`

> Разрешает использование unsafe-блоков (иначе `unsafe {}` → ошибка).

Пример:

```sg
pragma unsafe
fn main() { unsafe { poke(0xFFFF, 42); } }
```

---

### 📦 6) `pragma export`

> Указывает, что модуль предназначен для **внешнего вызова** (например, из C или Surge VM).

```sg
pragma export(c)
extern<Math> {
  pub fn add(a:int, b:int) -> int;
}
```

Позже можно использовать для:

* генерации заголовков (`.h`),
* экспорта в VM ABI,
* указания соглашения о вызовах (`export(c)`, `export(vm)`, `export(js)`).

---

### 🧠 7) `pragma feature(...)`

> Включает экспериментальные возможности языка.

```sg
pragma feature(async, generics_v2)
```

Механика:

* компилятор включает конкретные флаги внутри parser/sema,
* совместимо с future-guard системой версий.

---

### 🧩 8) `pragma version`

> Заявляет минимальную требуемую версию компилятора Surge.

```sg
pragma version "0.6.3"
```

Если версия компилятора меньше — ошибка на уровне frontend: `E_PRAGMA_VERSION_TOO_LOW`.

---

### 🔬 9) `pragma build`

> Параметры для сборщика, похожие на `go:build`.

```sg
pragma build os="linux", arch="x86_64"
```

Эта pragma читается **до** компиляции, на этапе discovery модулей.
Фронтенд может просто не включить файл в сборку, если условия не совпадают.

---

### ⚡️ 10) `pragma cache`

> Управляет кешированием модуля (например, для тяжёлых расчётов).

```sg
pragma cache persist=true, ttl="10m"
```

На старте — просто заглушка для build-системы, но в будущем можно реализовать:

* `persist=false` — всегда пересобирать,
* `ttl` — срок годности кеша.

---

## 🧩 Суммарно по M0–M2

| pragma            | Статус | Назначение                 |
| ----------------- | ------ | -------------------------- |
| `directive`       | ✅ M0   | Директивные модули         |
| `no_std`          | ✅ M1   | Без стандартной библиотеки |
| `strict`          | ✅ M1   | Строгий режим              |
| `unsafe`          | ✅ M1   | Разрешение unsafe          |
| `module <kind>`   | ✅ M1   | Тип модуля                 |
| `export(<abi>)`   | 🧩 M2  | Экспорт ABI                |
| `version "<ver>"` | 🧩 M2  | Требуемая версия Surge     |
| `feature(...)`    | 🧩 M2  | Включение экспериментов    |
| `build ...`       | 🧩 M2  | Build tags                 |
| `cache ...`       | 🧩 M2  | Управление кешем           |

---

## 🧱 Поведение в компиляторе

* Все pragma обрабатываются **на этапе парсинга файла**, до AST.
  Хранятся в `ModuleMeta`.
* CLI может их **переопределять** (например, `--no-strict` снимает `pragma strict`).
* Неявное объединение:

  * `pragma directive` автоматически включает `pragma module library`.
  * `pragma export(c)` включает `pragma unsafe` (разрешает FFI).
