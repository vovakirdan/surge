# Epic 15 Task 5: Naming Remainder

**Status:** complete (2026-07-12).

Order followed the plan: allowlist first (RV2-DEBT pointers, sync-point
names, retired-surface history notes in `crosses_deferred/`), then the
C3 decision, then the C2 headers.

- **C3 resolved as option (b), recorded deviation.** The plan preferred
  promoting the fixture matrix into a durable doc when 435 files carried
  row IDs; after the main sweep exactly two `ON-CROSS-N001` mentions
  survive, both inside the crosses_deferred retirement note where the ID
  IS the history being documented. A dedicated matrix document for two
  self-explanatory mentions would be indirection for its own sake.
- **C2**: the 8 remaining epic-numbered fixture headers rewritten to
  self-contained behavior sentences (goldens regenerated, comment-only
  churn); the last stragglers in `scripts/`, the core prelude comment,
  and a rules.toml header cleaned the same way.
- Final census: zero `Epic N` references outside `docs/` and commit
  messages; `naming-cleanup-plan.md` closed with the census and the
  policy section kept in force.
