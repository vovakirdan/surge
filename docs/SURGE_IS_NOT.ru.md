# SURGE_IS_NOT.md

> Surge is not here to win a cosplay contest.
> Surge is here to be Surge.

Surge уважает Rust, Go, TypeScript, Python и остальных.
Без них языка бы просто не было. Но он **не пытается быть их клоном**.

Этот файл — короткий путеводитель по тому, **чем Surge НЕ является**, с лёгкой самоиронией и маленькими код-примерами.

---

## Surge is not Rust

Да, мы знаем, что вы всё равно будете сравнивать.  
Это нормально. Мы тоже.

### Arrays: we know `[T; N]`, we chose `T[N]`

В Rust:

```rust
let xs: [i32; 3] = [1, 2, 3];
````

В Surge:

```sg
let xs: int32[3] = [1, 2, 3];
```

Мы знаем о синтаксисе `[T; N]`, но выбрали `T[N]`, потому что:

* Тип «по ощущениям» похож на указание **формы** (`int[3]`, `int[10][10]`),
* он лучше визуально сочетается с `T[]` и `Option<T>`/`Erring<T, E>`,
* и потому что автору просто так захотелось. Иногда этого достаточно.

### Result vs Erring: это не просто rename

В Rust:

```rust
fn parse(s: &str) -> Result<i32, Error> { ... }
```

В Surge:

```sg
tag Success<T>(T);

type Erring<T, E: ErrorLike> = Success(T) | E;

fn parse(s: string) -> Erring<int, Error> {  // или int!
    if (s == "42") { 
        return Success(42); 
    }
    let e: Error = { message: "bad", code: 1 };
    return e; // error идёт как есть
}
```

И sugar:

```sg
fn parse(s: string) -> int! { ... }  // == Erring<int, Error>
```

Мы отказались от прямого клона `Result<T, E>` потому что:

* хотим использовать **`Success<T>`** вместо `Ok<T>` — семантически честнее,
* хотим, чтобы **ошибка могла возвращаться напрямую**, без `Err(...)`,
* хотим один стандартный `T!` в типах (без зоопарка `T!CustomError!Whatever`),
* и потому что у нас уже есть `compare`, который хорошо дружит с tagged unions.

Surge не Rust: мы заимствовали идею «результат-или-ошибка», но сделали свою модель вокруг `Erring` и тегов.

### Option vs `T?`

В Rust:

```rust
fn head(xs: &[i32]) -> Option<i32> { ... }
```

В Surge:

```sg
tag Some<T>(T);
type Option<T> = Some(T) | nothing;

fn head<T>(xs: T[]) -> Option<T> {
    if (xs.len == 0) { return nothing; }
    return Some(xs[0]);
}

let maybe_num: int? = head([1, 2, 3]);  // == Option<int>
```

Опять же: идея знакомая, реализация — своя. И да, у нас **нет** `expr?`-оператора; если хочется магии, придётся писать `compare`.

### match vs compare

В Rust:

```rust
match result {
    Ok(v)  => println!("{}", v),
    Err(e) => println!("err: {}", e),
}
```

В Surge:

```sg
compare result {
    Success(v) => print("ok", v);
    err        => print("err", err);
}
```

Мы знаем про `match`. Просто назвали это `compare`, чтобы:

* подчеркнуть именно **сравнение формы/значения**,
* не тащить за собой ожидания Rust по exhaustiveness и паттернам (у нас свой план).

### Нет, последняя строка — это не `return`

В Rust можно молча воткнуть выражение в конец функции, забыть `;` — и это станет возвратом.

В Surge:

```sg
fn answer() -> int {
    return 42;
}
```

Да, обязательно `return`.
Да, даже если это последняя строка.
Да, даже если вам очень лень.

Кому вообще пришла в голову идея, что «строка без точки с запятой» должна означать возврат? Мы любим явность чуть больше, чем один сэкономленный символ.

### Конечно, `finally` можно заменить на `_`

В `compare` у нас есть специальное слово `finally`:

```sg
compare result {
    Success(v) => print(v);
    finally    => print("fallback");
}
```

Но если вам ближе `_` в духе Rust — пожалуйста:

```sg
compare result {
    Success(v) => print(v);
    _          => print("fallback");
}
```

`finally` — это просто более разговорный способ сказать «всё остальное».
Хотите строгости — используйте `_`. Хотите драматургии — берите `finally`.

### Traits vs Contracts + extern<T>

В Rust:

```rust
trait Display {
    fn fmt(&self) -> String;
}

impl Display for Point {
    fn fmt(&self) -> String { ... }
}
```

В Surge:

```sg
contract Display<T> {
    fn fmt(self: &T) -> string;
}

type Point = { x: int, y: int };

extern<Point> {
    fn fmt(self: &Point) -> string {
        return "(" + self.x + ", " + self.y + ")";
    }
}
```

Почему так:

* **contracts** — чисто структурные требования (если тип «подходит» — он подходит),
* `extern<T>` держит поведение рядом с типом, но не внутри,
* никаких иерархий, наследования, blanket-impl-магии — всё простое и видимое.

Surge не Rust: мы любим идею «trait-like» абстракций, но сделали их проще и менее магическими.

### Мы тоже не в восторге от дженериков

Честно: никто не просыпается утром с мыслью «как бы мне сегодня написать побольше `<T>`».

В Surge:

```sg
fn id<T>(x: T) -> T {
    return x;
}

let a = id(42);    // T выводится как int
let b = id("hi");  // T выводится как string
````

Там, где очевидно — компилятор выводит типы сам.
Там, где неочевидно — он честно говорит: «Я не уверен, напиши `::<int>` сам, пожалуйста».

```sg
fn make<T>() -> T;

let x = make();        // ошибка: нельзя вывести T по return-типу
let y = make::<int>(); // так уже понятно всем
```

Мы тоже не любим дженерики. Поэтому там, где «вроде очевидно», вы можете их опустить — пока не станет неочевидно. И вот тогда придётся договорить фразу до конца.

### Lifetimes vs boring ownership

У нас:

* `own T` — владею,
* `&T` — смотрю,
* `&mut T` — единственный, кто меняет.

Лайфтаймов как синтаксического зверя нет — за вас не будет прыгать зоопарк `'a`, `'static` и прочего. Borrow-правила есть, но они **лексические и очевидные**. Если надо закончить borrow раньше — есть явный `@drop`.

Мы знаем, что Rust-традиция сильная. Просто решили, что **ночной сон разработчика тоже важен**.

### Ой, кажется у нас нет `int128`

В Surge есть семейство динамических `int/uint/float` и фиксированные `int32`, `uint64` и друзья. `int128` пока не завезли.

Если в какой-то момент вам понадобился `int128`, то хороший вопрос:
**какую именно задачу вы пытались им решать?**

- криптография? возможно, вам нужен не `int128`, а хорошая библиотека;
- деньги? попробуйте фиксированную десятичную, а не битовый молоток;
- просто «красиво звучит»? тут мы бессильны.

---

## Surge is not TypeScript

Да, мы тоже знаем, что `interface Foo { ... }` удобно.
Поэтому у нас есть **contracts**, только без npm и `node_modules` в придачу.

### `interface` vs `contract`

В TypeScript:

```ts
interface User {
  id: number;
  name: string;
}
```

В Surge:

```sg
contract UserLike<T> {
    field id: int;
    field name: string;
}
```

Смысл похож, но:

* contracts живут на уровне **языка**, а не tsc-конфигурации,
* только строгие `field` и `fn` — без значений по умолчанию внутри,
* это **структурный контракт**, не класс.

### `as` vs `to`

TypeScript любит `value as Type`.  

В Surge:

```sg
let x: int32 = 300000;
let a: int   = x to int;    // нормально
let b: int16 = x to int16;  // может паникнуть, если не влезет
```

Мы используем `to`, потому что вы конвертируете `Egg to Bird`, а не `Egg as Bird`.
`as` звучит так, будто мы просто закрыли глаза и договорились.

И да, будьте осторожны: не все `Egg` могут быть `Bird`. Некоторые при приведении могут стать `Amphibian` (читай — вызвать рантайм-ошибку, если значение не помещается в целевой тип).

### type annotations: `name: Type`, не `Type name`

В TypeScript:

```ts
let user: User;
```

В Surge:

```sg
let user: User;
```

Да, здесь всё как в TS и Rust: `name: Type`. Это осознанный выбор — **тип после имени** позволяет:

* легче читать цепочки,
* проще воспринимать generic’и и модификаторы владения (`name: own T`, `arg: &mut Foo`).

И да, мы знаем, что можно было сделать `Type name`. Мы просто не стали.

### `Promise<T>` vs `Task<T>`

В TypeScript:

```ts
async function load(): Promise<Data> { ... }
await load();
```

В Surge:

```sg
async fn load() -> Data {
    ...
}

let task: Task<Data> = load();
let data = task.await();
```

`Task<T>` — обычный тип, `await()` — метод, а не отдельный оператор; модель async/await встроена в типы и структурированную конкуррентность, а не подвешена поверх всего.

### `any`, `unknown`, `never` vs осмысленные типы

В TypeScript вы можете написать `any` и просто **выключить** систему типов.

В Surge:

```sg
// Нет прямого аналога any
// Если очень нужно «что угодно» — это отдельная история и контракт.
```

Система типов не даёт единственного «чёрного хода», который магически обходит все проверки. Если хочется обмануть компилятор, придётся делать это честно и явно.

### Декораторы vs атрибуты

В TypeScript:

```ts
@Log
class Service { ... }
```

В Surge:

```sg
@pure
fn add(x: int, y: int) -> int { return x + y; }
```

У нас есть атрибуты, но они:

* **закрытый набор** (нельзя внезапно придумать `@Magic` и переписать пол языка),
* только намёки компилятору — без скрытых трансформаций,
* статически проверяются на совместимость и применимость.

Surge не TypeScript: мы любим структуру, но не любим магию во время компиляции.

---

## Surge is not Go

Go дал Surge очень много — простоту, инструментарий, философию «tooling как часть языка».
Но дальше пути расходятся.

### GC vs ownership

В Go:

```go
type User struct {
    ID   int
    Name string
}
```

и дальше всё решает GC.

В Surge:

```sg
type User = { id: int, name: string };

fn handle_user(user: own User) {
    // user будет освобождён, когда выйдет из области
}
```

Жизнь без GC:

* нет пауз,
* есть явное владение (`own T`, `&T`, `&mut T`),
* правила единообразны и подходят и для системного кода, и для high-level.

### Всё должно с чего-то начинаться

Нам очень понравилась идея Go, что у всего есть значение по умолчанию.

В Surge есть встроенный `default<T>() -> T`:

```sg
@intrinsic
fn default<T>() -> T;

let x: int = default<int>();  // 0
let ok: bool = default<bool>(); // false
```

И даже:

```sg
type Point = { x: int, y: int };

let p: Point = default<Point>(); // оба поля по умолчанию
```

Мы очень любим, когда «ничего» — это всё-таки **что-то осмысленное**.
Даже если это просто ноль. Или пустая строка. Или `nothing`.

### `error` vs `Erring<T, E>`

В Go:

```go
val, err := Do()
if err != nil {
    return err
}
```

В Surge:

```sg
fn do() -> int! {
    ...
}

let result = do();
compare result {
    Success(v) => print("ok", v);
    err        => print("err:", err);
}
```

Мы не распиливаем результат и ошибку через запятую — у нас один тип, который честно говорит «либо значение, либо ошибка».

### Tooling: похожий UX, другой смысл

`go fmt`, `go vet`, `go build` →

`surge fmt`, `surge diag`, `surge parse`, `surge tokenize`, `surge fix`.

Но:

* `diag` — это честный фронтенд с семантикой и диагностикой,
* есть режимы трассировки, Chrome Trace Viewer, ndjson для CI,
* всё заточено под прозрачность, а не только под «быстро собрать бинарь».

---

## Surge is not Python

Surge сильно вдохновлён ощущением от Python: «код читается как текст».
Но он не разделяет его любви к «пусть всё само разрулится в рантайме».

### Exceptions vs Erring

В Python:

```py
def parse(s: str) -> int:
    return int(s)  # ValueError по вкусу
```

В Surge:

```sg
fn parse(s: string) -> int! {
    if (s == "42") { return 42; }
    let e: Error = { message: "bad", code: 1 };
    return e;
}
```

У нас нет скрытых выбросов — всё в типе. Ошибка видна в сигнатуре, а не на проде.

### Да, у нас есть `nothing`

Python любит `None`, другие языки — `null`.  
У каждого своя философия «ничего».

В Surge есть честный отдельный тип `nothing` с единственным значением `nothing`.

```sg
fn do_nothing() -> nothing {
    return nothing;
}
```

Иногда вам просто нужно вернуть «ничего» — не «0», не `false`, не пустую строку.
Просто «здесь ничего нет», без подтекста.

`nothing` — это не ошибка и не особый случай.
Это просто отсутствие значения, которое честно видно в типе.

### Dynamic vs static (но без фанатизма)

* В Python типы — опциональны и легко игнорируемы.
* В Surge типы — базовый инструмент, но sugar (`T?`, `T!`, tagged unions, литеральные enum’ы) делает это терпимым для человека.

И да, Data Scientist, увидевший Surge, может сказать:
«Ты серьёзно считаешь это нормальным синтаксисом?» —
мы просто дадим ему посмотреть на Rust и сказать: «подожди, сейчас станет лучше».

### Магия dunder-методов vs честные magic-методы

В Python:

```py
class Vector:
    def __add__(self, other): ...
```

В Surge:

```sg
extern<Vector> {
    fn __add(self: &Vector, other: &Vector) -> Vector { ... }
}
```

У нас `__add`, `__index`, `__to` и прочие — тоже «магия», но:

* они видны как обычные функции,
* их можно читать, переопределять в `extern<T>`,
* built-in для примитивов запечатаны, но alias’ы можно расширять.

Surge не Python: мы любим читаемость, но в обмен просим немного дисциплины.

---

## Surge is not C++

Некоторые patterns пересекаются (RAII, отсутствие GC, системное программирование), но на этом сходство можно аккуратно сложить в коробку.

* Нет макро-препроцессора.
* Нет десятка правил про ODR, ADL и товарищей.
* Нет UB как нормально принятая норма.

Вместо этого:

* ownership-модель уровня Rust, но без синтаксиса lifetimes,
* строгая, но понятная система типов,
* атрибуты вместо жёсткой метапрограммной магии.

---

## Surge is not Java / C#

Мы не живём в мире классов и наследования.

* **нет классов** в привычном смысле — есть типы (`type`) и контракты (`contract`),
* **нет наследования поведения** — у нас есть расширение структур (`Base : { extra }`) и tagged unions,
* **нет «всё — объект»** — есть обычные значения с ownership-моделью.

Методы висят на `extern<T>`, а не внутри классов. Поля — просто поля, без геттеров/сеттеров на каждый вдох.

---

## Surge is not Zig

Мы знаем и любим идею «компактный, честный системный язык».
Но у нас другие акценты.

* У нас нет глобального, вездесущего `comptime` — мы строим **директивы** и будущие **макросы** как управляемый слой поверх языка.
* Мы сильно вкладываемся в **конкуррентность и диагностику**: структурные async-задачи, каналы, lock-контракты, трассировка.
* Мы чётко разделяем:

  * **атрибуты** — изменяют семантику и проверяются компилятором,
  * **директивы** — не меняют программу, а добавляют tooling (tests, benchmarks, docs).

---

## Surge is not Haskell

Да, мы знаем про монады.
Да, `Erring<T, E>` очень просится стать «ещё одной».
Нет, у нас нет `do`-нотации и лени по умолчанию.

* `compare` — простой pattern-matching, не теоретико-категорный ритуал.
* контракты — это **структурные интерфейсы**, а не typeclass-иерархии,
* async/await — это `Task<T>` и `.await()`, а не монада `IO` (по крайней мере, официально 😉).

---

## Surge is not C

C это низкоуровневый язык, который используется для написания системного ПО.
Surge это высокоуровневый язык, который используется для написания системного ПО.

Но да, enum у нас есть, и работают они как в C.

Хочу предупредить: язык Surge не задумывался как тот, на котором вы запустите ракету в космос. Но вы можете попробовать. Расскажите нам, если вам понравится.

---

## Surge is not “just a scripting language”

Surge — это **системный и прикладной язык**:

* без GC, с честным ownership, RAII и будущим LLVM-бэкендом;
* с async/await, задачами, каналами и структурированной конкуррентностью;
* с цельной историей: от токенов и AST до VM и, позже, нативного кода.

Но при этом:

* он старается быть читаемым,
* diagnostics стараются быть человечными,
* tracing даёт X-ray на ваш код, а не только stack trace.

## A tiny language tour (or what Surge actually IS)

Да, мы много говорили о том, кем Surge **не является**.  
Пора немного поговорить о том, что он всё-таки умеет.

### nothing: когда «ничего» значит «ничего»

У нас есть тип `nothing` и единственное значение `nothing`.

```sg
fn maybe_find(id: int) -> int? {
    if (id == 42) {
        return 42;
    }
    return nothing;
}
```

Когда вы видите `-> nothing` или `-> T?`, вы не гадаете «что здесь возвращают — `null`, `0` или пустую строку?».
Если написано `nothing`, это буквально «ничего». И это хорошо.

### Tuples: самые обычные, с `.0`, `.1`, `.2`

Tuples у нас не ищут смысла жизни — они просто группируют значения.

```sg
fn coords() -> (int, int) {
    return (10, 20);
}

let p: (int, int) = coords();
let x = p.0;
let y = p.1;
```

* `(int, int)` — тип,
* `(10, 20)` — значение,
* `.0`, `.1`, `.2` — доступ к полям.

Никаких магических record’ов, именованных полей и скрытых конструкторов.
Нужны имена — берите `type` и нормальную структуру, tuples — только для тех случаев, когда имя важнее контекста, чем полю.

### Наследование сложно. Пространства имён ещё сложнее.

Мы долго думали, как дать «ровно столько», чтобы:

* можно было расширять типы,
* не превращать язык в многословный зоопарк `class Base<T> : IThing, IWhatever`.

В Surge расширение выглядит так:

```sg
type Person = { name: string, age: int };

type Employee = Person : {
    id: uint,
    department: string,
};
```

* `Employee` наследует поля `Person`,
* можно добавить новые,
* но мы не заходим в бездну сложных иерархий.

Если вы поймали себя на том, что сильно скучаете по виртуальным таблицам и ромбовидному наследованию — возможно, вы просто устали и вам нужен выходной.

### Generics: мы тоже их не любим, но они полезные

Да, у нас есть generics. Они аккуратные и стараются не мешаться.

```sg
fn id<T>(x: T) -> T {
    return x;
}

let a = id(42);        // T=int
let b = id("hello");   // T=string
```

Там, где всё очевидно — типы выводятся:

```sg
fn head<T>(xs: T[]) -> Option<T> { ... }

let h = head([1, 2, 3]); // T=int
```

Там, где «очевидность» — это только в голове автора, а у компилятора нет хрустального шара — он честно попросит подсказку:

```sg
fn make<T>() -> T;

let x = make();        // error: не могу вывести T
let y = make::<int>(); // вот теперь всё ясно
```

Мы тоже не обожаем generics.
Поэтому там, где можно, даём вам их не писать.
А там, где без них уже слишком странно — просим написать сразу и явно.

### Числа: динамические и фиксированные, но без фетишизма

Surge различает:

* семейства `int`, `uint`, `float`,
* фиксированные `int32`, `uint64`, `float32`, `float64`,
* и при этом не пытается быть энциклопедией битовых размеров.

Если вы не пишете криптографическую библиотеку или низкоуровневый формат, обычно достаточно `int` и `float`.

Если вы вдруг ощущаете острую потребность в `int128`, посмотрите на код и спросите себя:
может, там всё-таки нужен другой инструмент?

### Directives: «зачем?» — «чтобы было удобно»

Директивы — это такие маленькие сценарии внутри кода: тесты, бенчмарки, доки, линты и прочие полезные вещи.

```sg
/// test:
/// Addition:
///     test.eq(add(2, 3), 5);
fn add(x: int, y: int) -> int { return x + y; }
```

Они:

* не меняют семантику программы,
* живут в комментариях `///`,
* могут запускаться через флаги `--directives` (`collect`, `gen`, `run`),
* и используют тот же самый Surge, а не отдельный мини-язык.

Зачем это всё?
Поверите на слово — **так удобнее**.
Если не верите — просто попробуйте.

### Три цикла: два `for` и один `while`

Мы не стали выбирать один «правильный» цикл.

У нас есть:

```sg
// C-style for
for (let i: int = 0; i < 10; i = i + 1) {
    ...
}

// for-in
for x: int in xs {
    ...
}

// while
while (cond) {
    ...
}
```

Да, три цикла.
Да, два вида `for`.
Да, мы периодически подумываем добавить ещё один — чисто ради коллекции.
Но пока держимся.

### 30+ атрибутов (которых вы почти не заметите)

В Surge довольно богатая система атрибутов: `@pure`, `@overload`, `@override`, `@entrypoint`, `@send`, `@nosend`, `@packed`, `@align`, `@readonly`, `@guarded_by`, `@nonblocking`, и ещё с добрый десяток.

Но:

* набор закрытый — нельзя просто придумать `@wizard`,
* каждый атрибут жёстко проверяется на применимость,
* большинство из них живёт там, где нужно библиотекам и рантайму, а не каждому второму пользователю.

Да, мы не любим магию.
Да, у нас при этом больше тридцати атрибутов.
Нет, они не делают за вас «нечто неописуемое».
Они просто помогают компилятору лучше понимать ваши намерения.

### Конвертация типов: `to` и честный `__to`

Как уже говорили, мы используем `to`:

```sg
let x: int32 = 300000;
let a: int   = x to int;    // fixed -> dynamic, безопасно
let b: int16 = x to int16;  // может паникнуть, если не влезет
```

Под капотом это вызывает `__to(self, target)` из `extern<T>` или встроенный intrinsic.

Так что:

* `Egg to Bird` — нормальная попытка,
* `Egg to Amphibian` — ваша личная ответственность,
* `int to int16` может в какой-то момент честно сказать: «нет, я не помещаюсь» и вызвать панику.

`__to` никогда не делает «немножко неправильный результат».
Он либо отдаёт корректное значение, либо рушит выполнение — но никогда не притворяется, что всё хорошо.

### Что же на самом деле делает `@pure`?

```sg
@pure
fn add(x: int, y: int) -> int {
    return x + y;
}
```

`@pure` — это не магический оптимизатор.

Она просто:

* говорит компилятору: «эта функция без побочных эффектов»,
* позволяет параноидальному режиму проверить, что вы не пишете в глобальные переменные и не делаете I/O,
* даёт зелёный свет агрессивным оптимизациям и кэшированию.

То есть `@pure` не делает функцию чистой.
Она лишь вежливо предупреждает: «если она окажется нечистой — это уже ваша проблема, но компилятор постарается заметить».

### `@overload`: да. А что не так?

```sg
fn print_int(x: int) { ... }

@overload
fn print_int(x: string) { ... }
```

Да, у нас есть `@overload`.
Да, мы в курсе всех мемов про перегрузку функций и боль overload resolution.

Мы просто:

* обязали первую версию функции быть без `@overload`,
* сделали чёткий и детерминированный алгоритм выбора кандидата,
* запретили смешивать `@override` и `@overload` в одном месте.

Если вы не злоупотребляете — всё работает просто.
Если злоупотребляете — это тоже своего рода коммуникация с будущим вами.

---

## Surge is also not:

Раз уж мы начали:

* Surge is not PHP — у нас нет `$` перед каждой переменной, но зато есть `@` перед атрибутами.
* Surge is not Brainfuck — хотя иногда можно так воспринять раннюю версию любой спецификации.
* Surge is not Excel — формулы оставим на потом, сначала закончим с компилятором.
* Surge is not YAML — пробелы важны, но не **настолько**.
* Surge is not Jira — у нас нет тикетов, только diagnostics с кодами.

И да, Surge — это даже не «следующий Rust», «следующий Go» или «следующий Zig».
Surge — это просто язык, который честно говорит:

> «Пиши код. Ошибаться можно.
> Я просто помогу тебе понять, *где* именно.»
