// Basic enum test

enum Color = {
    Red,
    Green,
    Blue,
}

enum Status: uint8 = {
    Unknown = 0,
    Started,
    Done,
}

pub enum Visibility = {
    Public,
    Private,
    Protected,
}

fn main() {
    let c = Color::Red;
    let s = Status::Done;
}
