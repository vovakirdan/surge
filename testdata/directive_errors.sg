// Illustrates common directive mistakes for surge tool diagnostics.

/// lint:
/// lint.eq(foo(), 0)
fn foo() -> int {
    return 1;
}

import directives::test;

/// test:
/// test.missing_helper()
fn bar() -> void {
    return;
}
