// Enum with explicit values

enum HttpStatus: uint16 = {
    Ok = 200,
    NotFound = 404,
    ServerError = 500,
}

enum Flags: uint32 = {
    None = 0,
    Read = 1,
    Write = 2,
    Execute = 4,
}
