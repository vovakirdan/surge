pragma module, no_std;

// Core Error type for recoverable errors.

pub type Error = { message: string, code: uint };

// Note: Erring type with ErrorLike contract is not included in golden tests
// as it requires more complete contract implementation.
