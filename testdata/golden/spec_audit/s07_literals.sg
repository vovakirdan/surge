// §7 Literals & Inference Audit

@entrypoint
fn main() {
    // §7.1 Numeric literal typing
    let i: int = 123;           // defaults to int
    let f: float = 1.5;         // defaults to float
    // let i32: int32 = 123:int32;  // suffix - fixed width not fully supported

    // §7.2 Range literals - SEMA test only, VM may not support
    // let r1 = [..];         // full range
    // let r2 = [1..];        // from 1
    // let r3 = [..5];        // to 5
    // let r4 = [1..5];       // from 1 to 5
    // let r5 = [1..=5];      // inclusive to 5

    // §7.3 Strings
    let s: string = "hello";
    // len() has issues in generic context
    // Indexing returns code point
    let ch: uint = s[0] to uint;
    print("s[0] code point: " + ch to string);

    // §7.4 String standard methods - test a few
    let text: string = "hello world";
    // contains, find, etc. may not be implemented
    // let has: bool = text.contains("world");

    // §7.5 Array standard methods
    let mut arr: int[] = [1, 2, 3];
    // push, pop etc. may have issues
    // arr.push(4);

    print("§7 Literals: PARTIAL");
    print("  - Numeric defaults: PASS");
    print("  - String indexing: PASS");
    print("  - Range literals: NOT TESTED");
    print("  - String methods: NOT TESTED");
    print("  - Array methods: NOT TESTED");
}
