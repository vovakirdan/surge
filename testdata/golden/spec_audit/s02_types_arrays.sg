// §2.2 Arrays Audit

@entrypoint
fn main() {
    // Growable array T[]
    let arr: int[] = [1, 2, 3];

    // Fixed-length array T[N] - spec says N must be constant literal
    let fixed: int[3] = [1, 2, 3];

    // Array indexing
    let first: int = arr[0];

    let length: uint = len(arr);

    print("Array first: " + first to string);
    print("Array length: " + length to string);
    print("§2.2 Arrays: PASS");
}
