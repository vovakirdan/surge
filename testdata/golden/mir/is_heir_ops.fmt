// Test: MIR lowering for is/heir operators.

type Base = { x: int };

type Child = Base: { y: int };

@entrypoint
fn main() -> int {
    let x: Child = { x: 1, y: 2 };
    let a: bool = x is Child;
    let b: bool = Child heir Base;
    if a && b {
        return 1;
    }
    return 0;
}
