
== HIR ==
module 

fn sum(arr: type#770) -> int (id=1, sym=680) {
  let mut total: int [copy] = 0
  {
    let mut __iter1: type#100 = iter_init(arr): type#100
    while true {
      let __next2: type#772 = iter_next(__iter1): type#772
      if tag_test(__next2, nothing): bool {
        break
      }
      let x: int [copy] = tag_payload(__next2, Some, 0): int
      (total = (total + x)): int
    }
  }
  return total
}

