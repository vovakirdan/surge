
== HIR ==
module 

fn sum(arr: type#1519) -> int (id=1, sym=1453) {
  let mut total: int [copy] = 0
  {
    let mut __iter1: type#189 = iter_init(arr): type#189
    while true {
      let __next2: type#1521 = iter_next(__iter1): type#1521
      if tag_test(__next2, nothing): bool {
        release_box __next2
        break
      }
      let x: int [copy] = tag_payload(__next2, Some, 0): int
      release_box __next2
      (total = __add(total, x)): int
    }
    release_cursor __iter1
  }
  return total
}

