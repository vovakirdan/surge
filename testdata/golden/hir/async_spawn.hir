
== HIR ==
module 

async fn inc(x: int [copy]) -> int (id=1, sym=558) {
  return (x + 1): int
}

async fn test() -> int (id=2, sym=559) {
  let task: type#311 = spawn inc(5): type#311: type#311
  return task.await(): int
}

