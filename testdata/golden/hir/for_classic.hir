
== HIR ==
module 

fn sum_to(n: int [copy]) -> int (id=1, sym=558) {
  let mut total: int [copy] = 0
  {
    let mut i: int [copy] = 0
    while (i < n): bool {
      (total = (total + i)): int
      (i = (i + 1)): int
    }
  }
  return total
}

