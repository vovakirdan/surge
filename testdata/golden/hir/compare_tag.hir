
== HIR ==
module 

fn unwrap_or_zero(x: type#1511) -> int (id=1, sym=1449) {
  return {
    let __cmp1: type#1511 = x
    if tag_test(__cmp1, Some): bool {
      let v: int [copy] = tag_payload(__cmp1, Some, 0): int
      release_box __cmp1
      return v
    }
    if tag_test(__cmp1, nothing): bool {
      drop __cmp1
      return 0
    }
  }: int
}

