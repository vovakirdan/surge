
== HIR ==
module 

fn unwrap_or_zero(x: type#261) -> int (id=1, sym=518) {
  return {
    let __cmp1: type#261 = x
    if tag_test(__cmp1, Some): bool {
      let v: int [copy] = tag_payload(__cmp1, Some, 0): int
      return v
    }
    if tag_test(__cmp1, nothing): bool {
      return 0
    }
    return default(): int
  }: int
}

