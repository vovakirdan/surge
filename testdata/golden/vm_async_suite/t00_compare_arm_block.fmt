@entrypoint
fn main() -> int {
    let r = (async { ret 1; }).await();
    compare r {
        Success(v) => { print("a"); print("v=" + (v to string)); };
        _ => { print("cancelled"); };
    };
    return 0;
}
