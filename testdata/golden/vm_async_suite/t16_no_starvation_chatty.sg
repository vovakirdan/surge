@entrypoint
fn main() -> int {
    let a = spawn async {
        let i = 0;
        while i < 100 {
            print("A");
            checkpoint().await();
            i = i + 1;
        }
        return 0;
    };
    let b = spawn async {
        let i = 0;
        while i < 3 {
            print("B");
            checkpoint().await();
            i = i + 1;
        }
        return 0;
    };
    let c = spawn async {
        let i = 0;
        while i < 3 {
            print("C");
            checkpoint().await();
            i = i + 1;
        }
        return 0;
    };

    let _ = a.await();
    let _ = b.await();
    let _ = c.await();
    return 0;
}
