error SEM3020 testdata/golden/hir_borrow/invalid/move_forbidden_when_borrowed.sg:4:21 cannot move 'x' while it is shared-borrowed

== HIR ==
module 

fn t() -> nothing (id=1, sym=1136) {
  let x: string = "\"hi\""
  let r: &string [&] = (& x): &string
  let y: string = x
  return
}

borrow edges:
  L2273(r) (&) borrows L2272(x) at 0:56-58 scope=S3
events:
  borrow_start L2273(r) -> L2272(x) (&) at 0:56-58 scope=S3 note="B1"
  move L2272(x) at 0:80-81 scope=S3 note="issue=frozen borrow=B1"
  borrow_end L2273(r) -> L2272(x) scope=S3 note="scope_end B1"
move plan:
  L2272(x): MoveForbidden (move blocked by frozen (B1))
  L2273(r): MoveCopy (copy type)
  L2274(y): MoveNeedsDrop (non-copy (drop))

