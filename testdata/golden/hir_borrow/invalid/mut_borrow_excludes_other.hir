error SEM3018 testdata/golden/hir_borrow/invalid/mut_borrow_excludes_other.sg:4:19 cannot take shared borrow of 'x' while an exclusive borrow is active

== HIR ==
module 

fn t() -> nothing (id=1, sym=636) {
  let mut x: int [copy] = 1
  let m: &mut int [&mut] = (&mut x): &mut int
  let r: &int [&] = (& x): &int
  return
}

borrow edges:
  L1273(m) (&mut) borrows L1272(x) at 0:55-61 scope=S3
events:
  borrow_start L1273(m) -> L1272(x) (&mut) at 0:55-61 scope=S3 note="B1"
  borrow_start _ -> L1272(x) at 0:81-83 scope=S3 note="issue=conflict_mut borrow=B1"
  borrow_end L1273(m) -> L1272(x) scope=S3 note="scope_end B1"
move plan:
  L1272(x): MoveCopy (copy type)
  L1273(m): MoveAllowed (non-copy reference)
  L1274(r): MoveCopy (copy type)

