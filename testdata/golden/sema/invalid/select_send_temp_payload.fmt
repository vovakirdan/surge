fn build() -> string {
    let mut s = "seed-";
    s = s + "x";
    return s;
}

async fn pump() -> nothing {
    let ch = make_channel::<string>(1);
    let rx = make_channel::<int>(1);
    let v = select {
        ch.send(own build()) => 1;
        rx.recv() => 2;
    };
    print("done\n");
}
