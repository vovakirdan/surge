error SEM3141 testdata/golden/sema/invalid/select_send_far_temp_payload.sg:9:17 select send payload must be a whole owned binding so the losing arms can reclaim it: bind the value first (`let job = ...;` then `send(own job)`)
