error SEM3208 testdata/golden/sema/invalid/task_body_no_value.sg:6:12 this async body never gives a value with `ret`, so its task yields nothing, but Task<int> is expected here
error SEM3208 testdata/golden/sema/invalid/task_body_no_value.sg:13:12 not every path of this async body ends in `ret`: the body yields int, so each path must give it a value with `ret <expr>;`
error SEM3208 testdata/golden/sema/invalid/task_body_no_value.sg:21:12 this async body never gives a value with `ret`, so its task yields nothing, but Task<int> is expected here
