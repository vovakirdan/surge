async fn pump() -> nothing {
    let ch = Channel::<string>::new(1:uint);
    let rx = Channel::<int>::new(1:uint);
    let mut s = "seed-";
    s = s + "x";
    let v = select {
        ch.send(own s) => 1;
        rx.recv() => 2;
    };
    print(s);
}
