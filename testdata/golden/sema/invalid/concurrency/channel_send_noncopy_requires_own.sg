// Method calls should report the actual ownership mismatch for non-Copy payloads.

type Item = { value: int };

@entrypoint
fn main() -> int {
    let ch: own Channel<Item> = Channel::<Item>::new(1:uint);
    let item: Item = { value = 1 };
    ch.send(item);
    return 0;
}
