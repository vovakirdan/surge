async fn pump() -> int {
    let ch = Channel::<string>::new(1:uint);
    let rx = Channel::<int>::new(1:uint);
    let mut s = "seed-";
    s = s + "x";
    let v = select {
        ch.send(own s) => { return 5; };
        rx.recv() => 2;
    };
    let _ = v;
    print(s);
    return 0;
}
