// Test: an argv default does not waive the exact FromArgv parser requirement
// Expected: SEM3124

type Point = {
    x: int,
    y: int,
};

@entrypoint("argv")
fn main(p: Point = Point { x = 0, y = 0 }) -> int {
    return p.x;
}
