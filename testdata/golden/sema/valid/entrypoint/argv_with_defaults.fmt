// Test: @entrypoint("argv") with some default params is valid
// Every param needs from_str; defaults are used only for absent argv positions.
@entrypoint("argv")
fn main(x: int, y: int = 5) -> int {
    return x + y;
}
