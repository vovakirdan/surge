// scenario: basic function types
fn accepts_fn_param(f: fn(int) -> int, x: int) {
    // Just declaring function type parameter works
    let g: fn(int) -> int = f;
}

fn double(n: int) -> int {
    return n * 2;
}

fn test_fn_type() {
    // Declaring function type variables
    let callback: fn(int) -> int;
    let action: fn();
    let transform: fn(string, int) -> bool;
}
