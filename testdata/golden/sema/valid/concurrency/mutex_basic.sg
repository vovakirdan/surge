// Basic Mutex usage patterns

pub async fn test_mutex_lock_unlock() -> nothing {
    let mut m: Mutex = Mutex.new();
    m.lock().await();
    // critical section
    m.unlock();
}

pub fn test_mutex_try_lock() -> bool {
    let mut m: Mutex = Mutex.new();
    return m.try_lock();
}
