// Validates &self method calls with field access on the receiver.

type Point = { x: int, y: int }

extern<Point> {
    fn sum(self: &Point) -> int {
        return self.x + self.y;
    }
}

fn main() -> int {
    let p: Point = Point { x = 2, y = 3 };
    return p.sum();
}
