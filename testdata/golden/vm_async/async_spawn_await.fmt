@entrypoint
fn main() -> int {
    let t = task async {
        checkpoint().await();
        return 41 + 1;
    };
    let r: int = compare t.await() {
        Success(v) => v;
        Cancelled() => 0;
    };
    print(r to string);
    return 0;
}
