@entrypoint
fn main() -> int {
    let r = (async {
        let _t = spawn async {
            checkpoint().await();
            print("child done");
            ret 0;
        };
        ret 1;
    }).await();

    compare r {
        Success(v) => print("parent=" + (v to string));
        Cancelled() => print("parent=cancelled");
    };
    return 0;
}
