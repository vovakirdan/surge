// Epic 11 Block 3 positive: Copy capture (C01)
fn add(x: int, y: int) -> int {
    return x + y;
}

fn start(n: int) crosses -> far Task<int> {
    let k: int = 3;
    return spawn on pool {
        ret add(n, k);
    };
}
