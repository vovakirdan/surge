// Epic 11 Block 3 positive: async spawn on (X02, crosses now inferred)
fn double(x: int) -> int {
    return x * 2;
}

async fn start(n: int) -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
