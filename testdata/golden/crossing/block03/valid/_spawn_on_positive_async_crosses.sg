// Epic 11 Block 3 positive: async and crosses combined (X02)
fn double(x: int) -> int {
    return x * 2;
}

async fn start(n: int) crosses -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
