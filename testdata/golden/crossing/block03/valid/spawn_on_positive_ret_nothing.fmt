// positive: spawn on with ret nothing
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<nothing> {
    return spawn on pool {
        let _ = double(n);
        ret nothing;
    };
}
