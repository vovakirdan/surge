// Epic 11 Block 3 positive: spawn on shard(id) (D03)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int, id: ShardId) crosses -> far Task<int> {
    return spawn on shard(id) {
        ret double(n);
    };
}
