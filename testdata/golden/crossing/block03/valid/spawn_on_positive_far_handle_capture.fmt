// positive: far handle captured by value
fn keep(c: far TcpConn) -> nothing {
    return nothing;
}

fn start(conn: far TcpConn) -> far Task<nothing> {
    return spawn on pool {
        ret keep(conn);
    };
}
