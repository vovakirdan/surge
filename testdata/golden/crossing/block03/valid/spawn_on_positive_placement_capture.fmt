// Epic 11 Block 3 positive: Placement (Copy) captured into block (C10)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int, other: Placement) -> far Task<int> {
    return spawn on pool {
        let p: Placement = other;
        let _ = p;
        ret double(n);
    };
}
