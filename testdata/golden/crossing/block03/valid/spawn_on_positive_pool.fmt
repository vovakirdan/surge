// Epic 11 Block 3 positive: spawn on pool (D02, S02, B01)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    let t = spawn on pool {
        ret double(n);
    };
    return t;
}
