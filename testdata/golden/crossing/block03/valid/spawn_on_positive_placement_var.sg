// positive: spawn on dst where dst: Placement
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    let dst: Placement = pool;
    return spawn on dst {
        ret double(n);
    };
}
