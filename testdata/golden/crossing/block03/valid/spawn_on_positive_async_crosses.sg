// positive: async spawn on (crosses now inferred)
fn double(x: int) -> int {
    return x * 2;
}

async fn start(n: int) -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
