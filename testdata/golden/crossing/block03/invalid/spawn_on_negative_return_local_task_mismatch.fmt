// EXPECT-DIAG: SEM3015
// negative: far Task returned as local Task
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> Task<int> {
    return spawn on distributed {
        ret double(n);
    };
}
