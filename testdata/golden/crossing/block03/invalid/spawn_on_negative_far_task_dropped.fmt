// EXPECT-DIAG: SEM3107
// negative: far Task dropped without await/return
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> nothing {
    let t: far Task<int> = spawn on distributed {
        ret double(n);
    };
    return nothing;
}
