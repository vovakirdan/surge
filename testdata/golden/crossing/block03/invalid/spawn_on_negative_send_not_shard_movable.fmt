// EXPECT-DIAG: SEM3169
// negative: @send but not @shard_movable
@send
type SendOnly = {
    id: int,
};

fn use_value(v: own SendOnly) -> int {
    return v.id;
}

fn start(v: own SendOnly) -> far Task<int> {
    return spawn on distributed {
        ret use_value(own v);
    };
}
