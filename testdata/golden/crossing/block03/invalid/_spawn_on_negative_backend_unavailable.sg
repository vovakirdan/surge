// EXPECT-DIAG: FUT7019
// EXPECT-STAGE: backend
// negative: spawn on in a synchronous context
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return spawn on distributed {
        ret double(n);
    };
}
