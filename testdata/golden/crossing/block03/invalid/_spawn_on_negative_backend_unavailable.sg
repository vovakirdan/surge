// EXPECT-DIAG: FUT7015
// Epic 11 Block 3 negative: spawn on backend unavailable (G01)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) crosses -> far Task<int> {
    return spawn on distributed {
        ret double(n);
    };
}
