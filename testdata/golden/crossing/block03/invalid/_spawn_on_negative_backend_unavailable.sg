// EXPECT-DIAG: FUT7015
// EXPECT-STAGE: backend
// negative: spawn on backend unavailable
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return spawn on distributed {
        ret double(n);
    };
}
