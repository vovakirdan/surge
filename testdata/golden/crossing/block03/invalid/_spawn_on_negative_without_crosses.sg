// EXPECT-DIAG: SEM3162
// Epic 11 Block 3 negative: spawn on without crosses (X03)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
