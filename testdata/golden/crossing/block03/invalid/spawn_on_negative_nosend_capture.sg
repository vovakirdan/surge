// EXPECT-DIAG: SEM3166
// negative: @nosend capture
@nosend
type LocalOnly = {
    id: int,
};

fn peek(v: own LocalOnly) -> int {
    return v.id;
}

fn start(v: own LocalOnly) -> far Task<int> {
    return spawn on pool {
        ret peek(own v);
    };
}
