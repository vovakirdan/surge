// EXPECT-DIAG: SEM3174
// negative: @local spawn on conflict
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return @local spawn on distributed {
        ret double(n);
    };
}
