// EXPECT-DIAG: SEM3174
// Epic 11 Block 3 negative: @local spawn on conflict (S07)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return @local spawn on distributed {
        ret double(n);
    };
}
