// EXPECT-DIAG: SEM3015
// negative: far Task assigned to local Task annotation
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> nothing {
    let t: Task<int> = spawn on distributed {
        ret double(n);
    };
    let _ = t;
    return nothing;
}
