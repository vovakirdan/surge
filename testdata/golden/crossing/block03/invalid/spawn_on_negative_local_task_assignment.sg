// EXPECT-DIAG: SEM3015
// Epic 11 Block 3 negative: far Task assigned to local Task annotation (T02)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> nothing {
    let t: Task<int> = spawn on distributed {
        ret double(n);
    };
    let _ = t;
    return nothing;
}
