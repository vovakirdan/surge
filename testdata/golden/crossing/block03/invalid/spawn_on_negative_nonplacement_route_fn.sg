// EXPECT-DIAG: SEM3154
// Epic 11 Block 3 negative: route function returning non-Placement (D09)
fn pick() -> int {
    return 0;
}

fn start() -> far Task<int> {
    return spawn on pick() {
        ret 1;
    };
}
