// EXPECT-DIAG: SEM3166
// Epic 11 Block 3 negative: @nosend capture (C06)
@nosend
type LocalOnly = {
    id: int,
};

fn peek(v: own LocalOnly) -> int {
    return v.id;
}

fn start(v: own LocalOnly) crosses -> far Task<int> {
    return spawn on pool {
        ret peek(own v);
    };
}
