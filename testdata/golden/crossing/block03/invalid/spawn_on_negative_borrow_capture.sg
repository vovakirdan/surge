// EXPECT-DIAG: SEM3165
// negative: shared borrow capture
type Job = {
    id: int,
};

fn read(r: &Job) -> int {
    return r.id;
}

fn start(job: Job) -> far Task<int> {
    let r: &Job = &job;
    return spawn on pool {
        ret read(r);
    };
}
