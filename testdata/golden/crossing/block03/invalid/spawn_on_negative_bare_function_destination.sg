// EXPECT-DIAG: SEM3156
// negative: bare function name destination
fn route_for(user_id: uint64) -> Placement {
    return distributed;
}

fn start() -> far Task<int> {
    return spawn on route_for {
        ret 1;
    };
}
