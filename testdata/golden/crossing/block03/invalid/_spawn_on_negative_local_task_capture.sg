// EXPECT-DIAG: SEM3168
// Epic 11 Block 3 negative: local Task<T> captured as own crossing value (B07)
async fn work() -> int {
    return 1;
}

fn start() crosses -> far Task<int> {
    let local: Task<int> = spawn work();
    return spawn on pool {
        ret local.await();
    };
}
