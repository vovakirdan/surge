// EXPECT-DIAG: SEM3165
// negative: mutable borrow capture
type Job = {
    id: int,
};

fn bump(r: &mut Job) -> int {
    return r.id;
}

fn start() -> far Task<int> {
    let mut job: Job = Job { id: 0 };
    let r: &mut Job = &mut job;
    return spawn on pool {
        ret bump(r);
    };
}
