// EXPECT-DIAG: SEM3169
// negative: @send-only crosses via spawn on
@send
type Job = {
    id: int,
};

fn run_job(job: own Job) -> int {
    return job.id;
}

fn run(job: own Job) -> far Task<int> {
    return spawn on distributed {
        ret run_job(own job);
    };
}
