// EXPECT-DIAG: SEM3165
// negative: borrow capture into on
type Job = {
    id: int,
};

fn read(r: &Job) -> int {
    return r.id;
}

fn run(job: Job) -> TaskResult<int> {
    let r: &Job = &job;
    return on pool {
        ret read(r);
    };
}
