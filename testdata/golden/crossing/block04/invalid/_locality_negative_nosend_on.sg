// EXPECT-DIAG: SEM3166
// Epic 11 Block 4 negative: @nosend via on (LOCALITY-010)
@nosend
type LocalOnly = {
    id: int,
};

fn peek(v: own LocalOnly) -> int {
    return v.id;
}

fn run(v: own LocalOnly) crosses -> TaskResult<int> {
    return on pool {
        ret peek(own v);
    };
}
