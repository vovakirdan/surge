// EXPECT-DIAG: SEM3174
// negative: @local spawn on
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> far Task<int> {
    return @local spawn on pool {
        ret double(n);
    };
}
