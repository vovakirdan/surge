// EXPECT-DIAG: SEM3167
// Epic 11 Block 4 negative: owned @shard_pinned capture into on (CAPTURE-012)
fn run(conn: own TcpConn) crosses -> TaskResult<nothing> {
    return on pool {
        conn.close();
        ret nothing;
    };
}
