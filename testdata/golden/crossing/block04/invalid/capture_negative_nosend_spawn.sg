// EXPECT-DIAG: SEM3086
// negative: @nosend capture into ordinary spawn
@nosend
type LocalOnly = {
    id: int,
};

async fn work(v: own LocalOnly) -> int {
    return v.id;
}

fn run(v: own LocalOnly) -> Task<int> {
    return spawn work(own v);
}
