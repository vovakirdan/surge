// EXPECT-DIAG: SEM3167
// Epic 11 Block 4 negative: own @shard_pinned into spawn on (PINNED-005)
fn run(conn: own TcpConn) crosses -> far Task<nothing> {
    return spawn on pool {
        conn.close();
        ret nothing;
    };
}
