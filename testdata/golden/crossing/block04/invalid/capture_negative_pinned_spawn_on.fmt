// EXPECT-DIAG: SEM3167
// negative: owned @shard_pinned capture into spawn on
fn run(conn: own TcpConn) -> far Task<nothing> {
    return spawn on pool {
        conn.close();
        ret nothing;
    };
}
