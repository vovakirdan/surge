// EXPECT-DIAG: SEM3169
// negative: @send-only crosses via on
@send
type Job = {
    id: int,
};

fn run_job(job: own Job) -> int {
    return job.id;
}

fn run(job: own Job) -> TaskResult<int> {
    return on pool {
        ret run_job(own job);
    };
}
