// EXPECT-DIAG: SEM3174
// negative: @local spawn on combines local and remote
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> far Task<int> {
    return @local spawn on distributed {
        ret double(n);
    };
}
