// EXPECT-DIAG: SEM3174
// Epic 11 Block 4 negative: @local spawn on combines local and remote (LOCALITY-012)
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> far Task<int> {
    return @local spawn on distributed {
        ret double(n);
    };
}
