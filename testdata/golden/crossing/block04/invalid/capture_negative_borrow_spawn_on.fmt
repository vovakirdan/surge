// EXPECT-DIAG: SEM3165
// Epic 11 Block 4 negative: borrow capture into spawn on (CAPTURE-008)
type Job = {
    id: int,
};

fn read(r: &Job) -> int {
    return r.id;
}

fn run(job: Job) -> far Task<int> {
    let r: &Job = &job;
    return spawn on pool {
        ret read(r);
    };
}
