// EXPECT-DIAG: SEM3165
// negative: borrow capture into spawn on
type Job = {
    id: int,
};

fn read(r: &Job) -> int {
    return r.id;
}

fn run(job: Job) -> far Task<int> {
    let r: &Job = &job;
    return spawn on pool {
        ret read(r);
    };
}
