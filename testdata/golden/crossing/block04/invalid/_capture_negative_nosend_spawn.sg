// EXPECT-DIAG: SEM3166
// Epic 11 Block 4 negative: @nosend capture into ordinary spawn (CAPTURE-010)
@nosend
type LocalOnly = {
    id: int,
};

async fn work(v: own LocalOnly) -> int {
    return v.id;
}

fn run(v: own LocalOnly) -> Task<int> {
    return spawn work(own v);
}
