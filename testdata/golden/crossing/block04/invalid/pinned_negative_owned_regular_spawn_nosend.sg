// EXPECT-DIAG: SEM3086
// Epic 11 Block 4 negative: @shard_pinned @nosend into ordinary spawn (PINNED-006)
async fn serve(conn: own TcpConn) -> nothing {
    return nothing;
}

fn run(conn: own TcpConn) -> Task<nothing> {
    return spawn serve(own conn);
}
