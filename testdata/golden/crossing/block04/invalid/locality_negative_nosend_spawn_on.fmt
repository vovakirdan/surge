// EXPECT-DIAG: SEM3166
// negative: @nosend via spawn on
@nosend
type LocalOnly = {
    id: int,
};

fn peek(v: own LocalOnly) -> int {
    return v.id;
}

fn run(v: own LocalOnly) -> far Task<int> {
    return spawn on pool {
        ret peek(own v);
    };
}
