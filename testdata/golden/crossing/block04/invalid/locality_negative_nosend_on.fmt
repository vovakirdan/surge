// EXPECT-DIAG: SEM3166
// negative: @nosend via on
@nosend
type LocalOnly = {
    id: int,
};

fn peek(v: own LocalOnly) -> int {
    return v.id;
}

fn run(v: own LocalOnly) -> TaskResult<int> {
    return on pool {
        ret peek(own v);
    };
}
