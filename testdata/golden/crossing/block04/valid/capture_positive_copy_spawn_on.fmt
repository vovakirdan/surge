// positive: Copy capture into spawn on
fn add(x: int, y: int) -> int {
    return x + y;
}

fn run(n: int) -> far Task<int> {
    let k: int = 4;
    return spawn on pool {
        ret add(n, k);
    };
}
