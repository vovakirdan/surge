// positive: @shard_movable + @send via ordinary spawn
@send
@shard_movable
type Job = {
    id: int,
};

async fn work(job: own Job) -> int {
    return job.id;
}

fn run(job: own Job) -> Task<int> {
    return spawn work(own job);
}
