// positive: inferred crossing effect propagates through calls.
fn inner(n: int) -> TaskResult<int> {
    return on pool {
        ret n;
    };
}

fn middle(n: int) -> TaskResult<int> {
    return inner(n);
}

fn outer(n: int) -> TaskResult<int> {
    return middle(n);
}
