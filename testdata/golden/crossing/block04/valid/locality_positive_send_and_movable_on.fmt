// positive: @send and @shard_movable crosses via on
@send
@shard_movable
type Job = {
    id: int,
};

fn run_job(job: own Job) -> int {
    return job.id;
}

fn run(job: own Job) -> TaskResult<int> {
    return on pool {
        ret run_job(own job);
    };
}
