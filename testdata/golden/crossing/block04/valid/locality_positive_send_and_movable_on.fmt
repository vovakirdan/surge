// Epic 11 Block 4 positive: @send and @shard_movable crosses via on (LOCALITY-001)
@send
@shard_movable
type Job = {
    id: int,
};

fn run_job(job: own Job) -> int {
    return job.id;
}

fn run(job: own Job) -> TaskResult<int> {
    return on pool {
        ret run_job(own job);
    };
}
