// Epic 11 Block 4 positive: far handle anchored by on (CAPTURE-005)
fn run(conn: far TcpConn) crosses -> TaskResult<nothing> {
    return on conn {
        conn.close();
        ret nothing;
    };
}
