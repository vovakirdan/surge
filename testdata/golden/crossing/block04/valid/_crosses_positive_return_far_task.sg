// Epic 11 Block 4 positive: crosses fn returning far Task (CROSSES-GRAMMAR-005)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) crosses -> far Task<int> {
    return spawn on distributed {
        ret double(n);
    };
}
