// Epic 11 Block 4 positive: spawn on inside crosses fn (CROSSES-EFFECT-002)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) crosses -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
