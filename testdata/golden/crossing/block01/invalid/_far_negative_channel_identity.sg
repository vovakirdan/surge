// EXPECT-DIAG: SEM3015
// Epic 11 Block 1 negative: Channel<far T> vs far Channel<T> identity (FAR-ID-007)
type Message = {
    id: int,
};

fn f(local: Channel<far Message>) -> nothing {
    let remote: far Channel<Message> = local;
    let _ = remote;
    return nothing;
}
