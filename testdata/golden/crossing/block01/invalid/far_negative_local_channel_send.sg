// EXPECT-DIAG: SEM3194
// negative: local channel send on far handle outside on
type Message = {
    id: int,
};

fn f(ch: far Channel<Message>, msg: own Message) -> nothing {
    ch.send(own msg);
    return nothing;
}
