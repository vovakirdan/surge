// EXPECT-DIAG: SEM3015
// negative: Channel<far T> vs far Channel<T> identity
type Message = {
    id: int,
};

fn f(local: Channel<far TcpConn>) -> nothing {
    let remote: far Channel<Message> = local;
    let _ = remote;
    return nothing;
}
