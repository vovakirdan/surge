// EXPECT-DIAG: SEM3130
// Epic 11 Block 1 negative: far handle is affine, not Copy (FAR-OWN-008)
fn take(x: own far TcpConn) -> nothing {
    return nothing;
}

fn f(h: own far TcpConn) -> nothing {
    let _ = take(own h);
    let again: far TcpConn = h;
    let _ = again;
    return nothing;
}
