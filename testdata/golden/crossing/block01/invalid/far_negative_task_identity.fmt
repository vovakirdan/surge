// EXPECT-DIAG: SEM3015
// negative: Task<far T> vs far Task<T> identity
fn f(local: Task<far TcpConn>) -> nothing {
    let remote: far Task<TcpConn> = local;
    let _ = remote;
    return nothing;
}
