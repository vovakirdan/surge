// EXPECT-DIAG: SEM3130
// negative: use after moving far handle
fn take(x: own far TcpConn) -> nothing {
    return nothing;
}

fn f(h: own far TcpConn) -> nothing {
    let _ = take(own h);
    return take(own h);
}
