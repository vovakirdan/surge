// Epic 11 Block 1 positive: exclusive borrow of far handle (FAR-PARSE-004, FAR-OWN-004)
fn edit(x: &mut far TcpConn) -> nothing {
    return nothing;
}

fn caller(h: far TcpConn) -> nothing {
    let mut local: far TcpConn = h;
    return edit(&mut local);
}
