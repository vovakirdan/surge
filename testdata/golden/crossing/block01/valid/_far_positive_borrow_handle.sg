// Epic 11 Block 1 positive: shared borrow of far handle + drop (FAR-PARSE-003, FAR-OWN-003, FAR-OWN-007)
fn view(x: &far TcpConn) -> nothing {
    return nothing;
}

fn caller(h: far TcpConn) -> nothing {
    let r: &far TcpConn = &h;
    let _ = view(r);
    @drop r;
    return nothing;
}
