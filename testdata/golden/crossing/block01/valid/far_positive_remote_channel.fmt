// positive: far Channel distinct from Channel<far T>
type Message = {
    id: int,
};

fn remote(ch: far Channel<Message>) -> nothing {
    return nothing;
}

fn local(ch: Channel<far Task<int>>) -> nothing {
    return nothing;
}
