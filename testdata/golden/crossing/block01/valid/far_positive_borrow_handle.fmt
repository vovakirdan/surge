// positive: shared borrow of far handle + drop
fn view(x: &far TcpConn) -> nothing {
    return nothing;
}

fn caller(h: far TcpConn) -> nothing {
    let r: &far TcpConn = &h;
    let _ = view(r);
    @drop r;
    return nothing;
}
