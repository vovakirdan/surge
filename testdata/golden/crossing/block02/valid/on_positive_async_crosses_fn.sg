// positive: async fn containing on (crosses keyword removed)
fn double(x: int) -> int {
    return x * 2;
}

async fn run(n: int) -> int {
    let r: TaskResult<int> = on pool {
        ret double(n);
    };
    return compare r {
        Success(v) => v;
        Cancelled() => 0;
    };
}
