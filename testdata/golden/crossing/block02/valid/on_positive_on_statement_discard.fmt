// positive: on used in statement position
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> nothing {
    on pool {
        let _ = double(n);
        ret nothing;
    };
    return nothing;
}
