// Epic 11 Block 2 positive: on pool (ON-DST-V001, ON-BODY-V001)
fn double(x: int) -> int {
    return x * 2;
}

fn score(n: int) crosses -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}
