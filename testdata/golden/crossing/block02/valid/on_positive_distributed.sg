// Epic 11 Block 2 positive: on distributed (ON-DST-V002)
fn double(x: int) -> int {
    return x * 2;
}

fn score(n: int) -> TaskResult<int> {
    return on distributed {
        ret double(n);
    };
}
