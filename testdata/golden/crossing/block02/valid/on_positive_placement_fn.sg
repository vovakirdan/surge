// positive: on route_for(id) returning Placement
fn double(x: int) -> int {
    return x * 2;
}

fn route_for(user_id: uint64) -> Placement {
    return distributed;
}

fn score(n: int, uid: uint64) -> TaskResult<int> {
    return on route_for(uid) {
        ret double(n);
    };
}
