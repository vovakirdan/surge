// Epic 11 Block 2 positive: crosses fn containing on (ON-CROSS-V001)
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) crosses -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}
