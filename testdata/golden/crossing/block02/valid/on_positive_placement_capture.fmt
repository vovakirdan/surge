// positive: Placement (Copy) captured into block
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int, other: Placement) -> TaskResult<int> {
    return on pool {
        let p: Placement = other;
        let _ = p;
        ret double(n);
    };
}
