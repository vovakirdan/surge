// EXPECT-DIAG: SEM3153
// negative: nested on placement
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> TaskResult<int> {
    return on pool {
        let inner: TaskResult<int> = on distributed {
            ret double(n);
        };
        ret double(n);
    };
}
