// EXPECT-DIAG: SEM3149
// negative: on result assigned to T instead of TaskResult<T>
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> nothing {
    let v: int = on pool {
        ret double(n);
    };
    let _ = v;
    return nothing;
}
