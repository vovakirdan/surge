// EXPECT-DIAG: SEM3165
// negative: shared borrow capture
type Request = {
    id: int,
};

fn read(r: &Request) -> int {
    return r.id;
}

fn run(req: Request) -> TaskResult<int> {
    let r: &Request = &req;
    return on pool {
        ret read(r);
    };
}
