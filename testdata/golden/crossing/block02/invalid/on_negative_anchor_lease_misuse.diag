error SEM3212 testdata/golden/crossing/block02/invalid/on_negative_anchor_lease_misuse.sg:4:29 an anchored `send` of `int` must give a captured binding away: the ring takes the value's only reference and the receiving shard may already hold it before this block ends, so the block cannot keep a reference of its own, and it cannot take a fresh one here either (a parked send re-enters the block from the top and would take it again)
error SEM3210 testdata/golden/crossing/block02/invalid/on_negative_anchor_lease_misuse.sg:4:44 the `on` block holds this handle as a lease for its channel operation only; it cannot be bound, passed on or returned inside the block -- the handle stays with the caller
