// EXPECT-DIAG: SEM3210
// negative: the anchor of an `on` block is a lease, not a value the block may bind
fn run(ch: far Channel<int>) -> nothing {
    let _ = on ch { ch.send(1); let held = ch; ret nothing; };
    return nothing;
}
