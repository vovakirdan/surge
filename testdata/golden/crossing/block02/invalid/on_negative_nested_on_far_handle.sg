// EXPECT-DIAG: SEM3153
// negative: nested on far handle
fn run(ch: far Channel<int>, conn: far TcpConn) -> TaskResult<nothing> {
    return on ch {
        let inner: TaskResult<nothing> = on conn {
            conn.close();
            ret nothing;
        };
        ret nothing;
    };
}
