// EXPECT-DIAG: FUT7014
// EXPECT-STAGE: backend
// negative: on placement crossing backend unavailable
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}
