// EXPECT-DIAG: FUT7014
// Epic 11 Block 2 negative: on placement crossing backend unavailable (ON-GATE-N001)
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) crosses -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}
