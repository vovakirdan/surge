// EXPECT-DIAG: FUT7019
// EXPECT-STAGE: backend
// negative: on placement crossing in a synchronous context
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}
