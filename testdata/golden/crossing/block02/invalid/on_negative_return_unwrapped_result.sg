// EXPECT-DIAG: SEM3149
// negative: on result returned from -> T function
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> int {
    return on pool {
        ret double(n);
    };
}
