// EXPECT-DIAG: SEM3149
// Epic 11 Block 2 negative: on result returned from -> T function (ON-BODY-N004)
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> int {
    return on pool {
        ret double(n);
    };
}
