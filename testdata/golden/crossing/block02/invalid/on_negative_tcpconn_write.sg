// EXPECT-DIAG: SEM3151
// negative: remote write through far TcpConn
fn run(conn: far TcpConn, buf: &byte[]) -> TaskResult<nothing> {
    return on conn {
        conn.write(buf);
        ret nothing;
    };
}
