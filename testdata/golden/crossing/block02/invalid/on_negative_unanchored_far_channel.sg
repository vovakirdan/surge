// EXPECT-DIAG: SEM3150
// negative: unanchored far channel operation
@shard_movable
type Message = {
    id: int,
};

fn run(a: far Channel<Message>, b: far Channel<Message>, msg: own Message) -> TaskResult<nothing> {
    return on a {
        b.send(own msg);
        ret nothing;
    };
}
