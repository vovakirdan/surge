// EXPECT-DIAG: SEM3152
// negative: on where suspension is illegal
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> TaskResult<int> {
    blocking {
        return on pool {
            ret double(n);
        };
    };
}
