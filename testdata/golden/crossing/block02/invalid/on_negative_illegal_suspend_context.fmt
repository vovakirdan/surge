// EXPECT-DIAG: SEM3152
// negative: on where suspension is illegal
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> Task<TaskResult<int>> {
    return blocking {
        ret on pool {
            ret double(n);
        };
    };
}
