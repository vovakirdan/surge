// EXPECT-DIAG: FUT7014
// EXPECT-STAGE: backend
// Epic 12 Task 2 negative: on far handle backend unavailable
@shard_movable
type Message = {
    id: int,
};

fn send_job(ch: far Channel<Message>, msg: own Message) -> TaskResult<nothing> {
    return on ch {
        ch.send(own msg);
        ret nothing;
    };
}
