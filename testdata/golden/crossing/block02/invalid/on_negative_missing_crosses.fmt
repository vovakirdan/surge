// EXPECT-DIAG: SEM3162
// Epic 11 Block 2 negative: on in non-crosses function (ON-CROSS-N001)
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}
