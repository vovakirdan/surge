// EXPECT-DIAG: SEM3152
// Epic 11 Block 2 negative: on where suspension is illegal (ON-CROSS-N002)
fn double(x: int) -> int {
    return x * 2;
}

fn run(n: int) -> TaskResult<int> {
    blocking {
        return on pool {
            ret double(n);
        };
    };
}
