error SEM3168 testdata/golden/crossing/block02/invalid/on_negative_array_view_element_capture.sg:12:23 `xs` cannot cross a shard boundary: its element `v` is a view of another array, and the elements that view windows live in a buffer this shard keeps, reads through and frees. Hold elements that own their own storage -- replace the view with an array holding a copy of every element in the window -- and cross that
