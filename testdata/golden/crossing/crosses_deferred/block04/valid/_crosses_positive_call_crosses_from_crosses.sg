// positive: crosses fn calling crosses fn
fn double(x: int) -> int {
    return x * 2;
}

fn inner(n: int) crosses -> TaskResult<int> {
    return on pool {
        ret double(n);
    };
}

fn outer(n: int) crosses -> TaskResult<int> {
    return inner(n);
}
