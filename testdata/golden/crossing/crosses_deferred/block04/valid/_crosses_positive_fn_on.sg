// Epic 11 Block 4 positive: crosses fn with on (CROSSES-GRAMMAR-001, CROSSES-EFFECT-001)
fn double(x: int) -> int {
    return x * 2;
}

fn route(n: int) crosses -> TaskResult<int> {
    return on distributed {
        ret double(n);
    };
}
