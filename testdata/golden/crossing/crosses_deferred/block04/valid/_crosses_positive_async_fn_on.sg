// positive: async fn crosses with on
fn double(x: int) -> int {
    return x * 2;
}

async fn route(n: int) crosses -> int {
    let r: TaskResult<int> = on pool {
        ret double(n);
    };
    return compare r {
        Success(v) => v;
        Cancelled() => 0;
    };
}
