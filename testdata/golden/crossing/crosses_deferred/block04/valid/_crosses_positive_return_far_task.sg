// positive: crosses fn returning far Task
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) crosses -> far Task<int> {
    return spawn on distributed {
        ret double(n);
    };
}
