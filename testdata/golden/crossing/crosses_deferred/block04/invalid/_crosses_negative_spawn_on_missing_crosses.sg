// EXPECT-DIAG: SEM3162
// negative: spawn on in non-crosses fn
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
