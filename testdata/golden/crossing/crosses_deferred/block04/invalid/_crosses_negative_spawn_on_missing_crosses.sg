// EXPECT-DIAG: SEM3162
// Epic 11 Block 4 negative: spawn on in non-crosses fn (CROSSES-EFFECT-008)
fn double(x: int) -> int {
    return x * 2;
}

fn start(n: int) -> far Task<int> {
    return spawn on pool {
        ret double(n);
    };
}
