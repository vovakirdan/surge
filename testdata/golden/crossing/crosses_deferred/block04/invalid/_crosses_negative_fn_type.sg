// EXPECT-DIAG: SYN2036
// Epic 11 Block 4 negative: crosses function-type syntax (CROSSES-GRAMMAR-015)
fn route(req: int) -> int {
    return req;
}

fn f() -> nothing {
    let cb: crosses fn(int) -> int = route;
    let _ = cb;
    return nothing;
}
