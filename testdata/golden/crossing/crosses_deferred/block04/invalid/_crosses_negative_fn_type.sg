// EXPECT-DIAG: SYN2036
// negative: crosses function-type syntax
fn route(req: int) -> int {
    return req;
}

fn f() -> nothing {
    let cb: crosses fn(int) -> int = route;
    let _ = cb;
    return nothing;
}
