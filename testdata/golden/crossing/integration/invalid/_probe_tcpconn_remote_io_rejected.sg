// EXPECT-DIAG: SEM3151
// stdlib-facing probe: remote TcpConn I/O through a far handle stays rejected.
fn run(conn: far TcpConn, buf: &mut byte[]) -> TaskResult<nothing> {
    return on conn {
        conn.read(buf);
        ret nothing;
    };
}
