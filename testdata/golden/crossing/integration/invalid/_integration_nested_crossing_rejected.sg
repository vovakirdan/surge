// EXPECT-DIAG: SEM3153
// Epic 12 Task 4 probe: nested crossing remains invalid under Epic 11.
fn run(dst: Placement) -> far Task<int> {
    return spawn on dst {
        let r: TaskResult<int> = on dst {
            ret 1;
        };
        ret 2;
    };
}
