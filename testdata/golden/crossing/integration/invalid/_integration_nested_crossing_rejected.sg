// EXPECT-DIAG: SEM3153
// negative probe: a crossing block nested inside another crossing block is rejected.
fn run(dst: Placement) -> far Task<int> {
    return spawn on dst {
        let r: TaskResult<int> = on dst {
            ret 1;
        };
        ret 2;
    };
}
