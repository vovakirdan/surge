// integration: a valid `spawn on` handle is consumed by `far Task.await`.
fn run(dst: Placement) -> TaskResult<int> {
    let task: far Task<int> = spawn on dst {
        ret 3;
    };
    return task.await();
}
