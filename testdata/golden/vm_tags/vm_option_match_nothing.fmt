fn unwrap_or_zero(x: int?) -> int {
    return compare x {
        Some(v) => v;
        nothing => 0;
    };
}

@entrypoint
fn main() -> int {
    let x: int? = nothing;
    return unwrap_or_zero(x);
}
