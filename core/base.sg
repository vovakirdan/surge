// Core algebraic data types used by the standard library.

// Tag constructors
tag Some<T>(T);
tag Ok<T>(T);
tag Error<E>(E);

// Option/Result based on tags. Absence is represented by `nothing`.
type Option<T> = Some(T) | nothing;
type Result<T, E> = Ok(T) | Error(E);
